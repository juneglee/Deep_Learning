# 외부 모듈
#
# 사이키-런(scikit-learn), 텐서플로우(Tensorflow), 장고(Django) , 플라스크(Flask) 등처럼
# 다른 사람이 만들어 보포하는 모듈을 의미한다 

# pip install 모듈 이름 (window + R -> cmd -> 명령)
# pip install beautifulsoup4
# pip install flask 
